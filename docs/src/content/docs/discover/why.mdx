---
title: Why this extension?
description: "The problem SmartTab Organizer solves, its three pillars (group, deduplicate, save) and its real differentiator: the group name extracted from the URL or title, locally."
---

import { Aside, CardGrid, LinkCard, Steps } from '@astrojs/starlight/components';

<Aside type="tip" title="Quick start">
<Steps>
1. Install the extension from the [Chrome Web Store or Firefox Add-ons](/discover/installation).
2. Open the Options and [import a rule pack](/reference/categories-and-packs), the fastest way to explore the extension.
3. Middle-click a link: the new tab automatically joins its group.
</Steps>
</Aside>

Some evenings you come back to your browser in a sorry state, packed with tabs and unsure which ones are still useful. You would love to keep everything, all the time, but that is not always possible.

SmartTab Organizer automatically groups your tabs based on rules you define, avoids duplicates, and lets you save and restore your workspaces as named sessions.

## Three pillars

The extension is built around three complementary functions.

<CardGrid>
  <LinkCard
    title="Automatic grouping"
    href="/guides/group-tabs"
    description="Tabs opened from the same parent land in a colored Chrome group, with no manual action."
  />
  <LinkCard
    title="Deduplicate"
    href="/guides/deduplicate"
    description="Three match modes (exact URL, URL without ignored params, includes) close duplicates while keeping the most relevant tab."
  />
  <LinkCard
    title="Save your sessions"
    href="/guides/sessions"
    description="Capture your browser state, pin contexts you come back to often, restore them in one click."
  />
</CardGrid>

## Everything local, nothing goes online

Existing grouping extensions require a fixed group name, typed by hand, with one rule per value. SmartTab Organizer extracts the name straight from the URL or title using a regular expression: a group per repository, per ticket or per client is created automatically, without multiplying rules. For example, a repository URL such as `github.com/EspritVorace/smart-tab-organizer` produces a group named after the repository, with no rule dedicated to that specific repository.

Grouping relies on regular expressions run locally, and all data lives in `browser.storage.local`. No data leaves the browser, and no AI, local or remote, is involved.

<Aside type="note">
If you sync your Chrome or Firefox profile, the extension stays compatible (data follows the profile), but no network communication is ever initiated by the extension itself.
</Aside>

## Who it is for

The extension was built for use cases where the browser is a work tool:

- **Development**: one tab per issue, one group per repository, one workspace per client.
- **Support and operations**: one group per ticket (Jira, ServiceNow, Linear), even when the incident is tracked across several tools in parallel.
- **Research and monitoring**: themed sessions you reopen every week, without having to rebuild the stack.
- **Everyday life**: separate work and personal contexts via distinct [workspaces](/guides/workspaces), each with its own accent color.

## Next steps

<CardGrid>
  <LinkCard title="Install" href="/discover/installation" description="Chrome Web Store, Firefox Add-ons, or a local build from the sources." />
  <LinkCard title="Interface tour" href="/discover/interface-tour" description="Popup and Options page: what you see on first launch." />
  <LinkCard title="The home page" href="/discover/home-page" description="Onboarding, pinned profiles, quick actions, mini-stats." />
</CardGrid>
