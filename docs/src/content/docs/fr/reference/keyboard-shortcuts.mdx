---
title: Raccourcis clavier
description: Tous les raccourcis clavier disponibles dans SmartTab Organizer, groupés par surface. Les raccourcis globaux peuvent être personnalisés depuis les paramètres d'extensions du navigateur.
---

{/* Cette page est générée depuis src/shortcuts/registry.ts. Lancez pnpm shortcuts:doc pour la mettre à jour. */}

Tous les raccourcis clavier disponibles dans SmartTab Organizer, groupés par surface. Les raccourcis globaux peuvent être personnalisés depuis les paramètres d'extensions du navigateur.

## Page d'accueil \{#list-home}

| Raccourci | Action |
| --- | --- |
| `←` ou `→` | Déplacer le focus entre les cartes |
| `Home` ou `End` | Aller au premier ou au dernier élément |
| `Tab` | Aller à la section suivante |
| `Enter` | Activer l'élément focus |
| `O` | Organiser tous les onglets |
| `S` | Enregistrer un instantané de la fenêtre courante |
| `N` | Créer une nouvelle règle de domaine |
| `I` | Aller à Import / Export |
| `T` | Aller aux statistiques |

### Carte session (focus) \{#session-card}

| Raccourci | Action |
| --- | --- |
| `R` | Restauration personnalisée (assistant) |
| `Shift + R` | Ajouter la session à la fenêtre courante |
| `Alt + R` | Remplacer les onglets de la fenêtre |
| `Alt + Shift + R` | Ouvrir la session dans une nouvelle fenêtre |
| `U` | Rafraîchir la session avec les onglets de la fenêtre |
| `→` | Déplier la liste des onglets de la session |
| `←` | Replier la liste des onglets de la session |

## Liste des règles \{#list-rules}

| Raccourci | Action |
| --- | --- |
| `↑` ou `↓` | Déplacer le focus entre les cartes |
| `N` | Créer un nouvel élément |
| `E` | Modifier l'élément focus |
| `Space` | Cocher ou décocher la règle focus |
| `T` | Activer ou désactiver la règle focus |
| `Delete` | Supprimer l'élément focus |
| `Mod + ↑` | En premier dans la liste |
| `Mod + ↓` | En dernier dans la liste |
| `Mod + Shift + ↑` | Première règle du domaine |
| `Mod + Shift + ↓` | Dernière règle du domaine |
| `Space` | Réorganiser via la poignée (puis flèches pour déplacer, Échap pour annuler) |
| `→` | Déplier le groupe focus |
| `←` | Replier le groupe focus |
| `Space` | Cocher ou décocher tout le groupe |

## Liste des sessions \{#list-sessions}

| Raccourci | Action |
| --- | --- |
| `↑` ou `↓` | Déplacer le focus entre les cartes |
| `N` | Créer un nouvel élément |
| `E` | Modifier l'élément focus |
| `Delete` | Supprimer l'élément focus |
| `P` | Épingler ou désépingler la session focus |
| `Mod + ↑` | En premier dans la liste |
| `Mod + ↓` | En dernier dans la liste |
| `A` | Archiver la session |
| `PageDown` | Aller à la section de sessions suivante |
| `PageUp` | Aller à la section de sessions précédente |
| `Space` | Réorganiser via la poignée (puis flèches pour déplacer, Échap pour annuler) |

### Carte session (focus) \{#session-card}

| Raccourci | Action |
| --- | --- |
| `R` | Restauration personnalisée (assistant) |
| `Shift + R` | Ajouter la session à la fenêtre courante |
| `Alt + R` | Remplacer les onglets de la fenêtre |
| `Alt + Shift + R` | Ouvrir la session dans une nouvelle fenêtre |
| `U` | Rafraîchir la session avec les onglets de la fenêtre |
| `→` | Déplier la liste des onglets de la session |
| `←` | Replier la liste des onglets de la session |

## Statistiques \{#list-stats}

| Raccourci | Action |
| --- | --- |
| `PageDown` | Aller à l'onglet de statistiques suivant |
| `PageUp` | Aller à l'onglet de statistiques précédent |

## Liste d'exploration \{#list-exploration}

| Raccourci | Action |
| --- | --- |
| `↑` ou `↓` | Déplacer le focus entre les cartes |
| `F` puis `A` | Filtre : toutes les capacités |
| `F` puis `D` | Filtre : découvertes |
| `F` puis `T` | Filtre : à découvrir |
| `F` puis `N` | Filtre : non possibles |
| `G` | Aller où utiliser la capacité ciblée |
| `H` | Ouvrir la documentation de la capacité ciblée |
| `Space` | Marquer ou démarquer la capacité ciblée comme découverte |

## Import / Export \{#importexport}

| Raccourci | Action |
| --- | --- |
| `I` puis `R` | Importer les règles |
| `I` puis `S` | Importer les sessions |
| `I` puis `W` | Importer les espaces de travail |
| `E` puis `R` | Exporter les règles |
| `E` puis `S` | Exporter les sessions |
| `E` puis `W` | Exporter les espaces de travail |

## Liste des espaces \{#list-workspaces}

| Raccourci | Action |
| --- | --- |
| `N` | Créer un nouvel élément |
| `E` | Modifier l'élément focus |
| `Delete` | Supprimer l'élément focus |

## Paramètres \{#list-settings}

| Raccourci | Action |
| --- | --- |
| `↑` ou `↓` | Déplacer le focus entre les paramètres |

## Changement d'espace \{#workspace}

| Raccourci | Action |
| --- | --- |
| `W` puis `N` | Passer à l'espace suivant |
| `W` puis `P` | Passer à l'espace précédent |
| `W` puis `L` | Passer au dernier espace utilisé |

## Page d'options \{#options}

| Raccourci | Action |
| --- | --- |
| `M` puis `H` | Aller à l'accueil |
| `M` puis `R` | Aller aux règles |
| `M` puis `S` | Aller aux sessions |
| `M` puis `T` | Aller aux statistiques |
| `M` puis `E` | Aller au référentiel d'exploration |
| `M` puis `I` | Aller à Import / Export |
| `M` puis `C` | Aller aux réglages |
| `M` puis `W` | Aller aux espaces de travail |
| `/` | Focus sur le champ de recherche |
| `Escape` | Effacer la recherche ou fermer le panneau |
| `?` | Afficher ce panneau de raccourcis |
| `F1` | Ouvrir la documentation de la page courante |

## Popup \{#popup}

| Raccourci | Action |
| --- | --- |
| `S` | Enregistrer un snapshot |
| `R` | Ouvrir les sessions |
| `O` | Organiser tous les onglets |
| `P` | Ouvrir les options de l'extension |
| `?` | Afficher ce panneau de raccourcis |

## Globaux navigateur \{#global}

| Raccourci | Action |
| --- | --- |
| `Ctrl + Shift + 1` | Ouvrir la popup de l'extension |
| Non défini | Organiser tous les onglets |
| Non défini | Enregistrer la fenêtre comme session |
| Non défini | Passer à l'espace suivant |
| Non défini | Passer à l'espace précédent |
| Non défini | Passer au dernier espace utilisé |
| `D` | Changer de thème (clair, sombre, système) |

